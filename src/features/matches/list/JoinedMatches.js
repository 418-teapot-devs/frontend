import { Matches } from "./Matches"

export const JoinedMatches = (height) => {
  return (
    <Matches
      matchType="joined"
      onClick={() => {}}
      buttontext="Detalles"
      height={height}
      title="Partidas compartidas"
    />
  )
}
