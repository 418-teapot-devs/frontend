import { Matches } from "./Matches"

export const CreatedMatches = (height) => {
  return (
    <Matches
      matchType="created"
      onClick={() =>{}}
      buttontext="Iniciar"
      height={height}
      title="Mis partidas"
    />
  )
}