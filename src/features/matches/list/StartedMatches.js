import { Matches } from "./Matches"

export const StartedMatches = ({height}) => {
  return (
    <Matches
      matchType="started"
      onClick={() =>{}}
      buttontext="Detalles"
      height={height}
      title="Historial de partidas"
    />
  )
}